eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: zuul-server
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      label: master
      name: zuul-server